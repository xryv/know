/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'via.placeholder.com', 
      'iconarchive.com', 
      'source.unsplash.com',
      // Add other domains if needed
    ],
  },
};

export default nextConfig;
